# Baseline: 2026-08-09

This baseline was captured read-only after WARPi was powered on and became reachable over the approved private management path.

## System

- Hostname: `WARPi`
- Hardware: Raspberry Pi 4 Model B Rev 1.5
- OS: Debian GNU/Linux 13
- Kernel: `6.18.34+rpt-rpi-v8`
- Architecture: `aarch64`
- CPU: 4x Cortex-A72
- Memory: about 3.7 GiB total
- Root disk: about 116 GiB, about 8% used at baseline
- Failed systemd units: none

## Peripherals

Observed:

- u-blox GPS receiver
- MediaTek MT7612U 802.11a/b/g/n/ac wireless adapter

Not attached during clean baseline:

- WiFi Pineapple NANO

## Network

WARPi booted into normal/trusted-client mode.

- Primary network: `wlan0`
- Trusted Wi-Fi: connected
- Internet: available
- Tailscale: connected
- Ethernet: present but disconnected
- Dedicated wireless adapter: present as `wlan1`, down in normal mode
- Docker bridge: present, link down

## Services

Active/running highlights:

- `warpi-kernel.service`
- `warpi-ui.service`
- `ssh.service`
- `tailscaled.service`
- `gpsd.service`
- `bettercap.service`
- `docker.service`
- `containerd.service`
- `NetworkManager.service`

Inactive/disabled:

- `kismet.service`

## Current WARPi State

`kernel-report --json` reported:

- Mode: `NORMAL`
- State: `CONNECTED`
- Reason: `trusted_wifi_and_internet_available`

GPS:

- device present
- GPSD running
- searching, no fix at baseline

Power telemetry:

- unavailable

Mission Control:

- enabled
- unhealthy/offline from WARPi at baseline
- PostgreSQL/Redis state unknown from WARPi

## Drift

The documentation/tooling still references legacy mode scripts:

- `<legacy-user-home>/scripts/fieldmode`
- `<legacy-user-home>/scripts/normalmode`
- `<legacy-user-home>/scripts/normalmode-worker`

Those paths were missing at baseline and are under stale-reference investigation.

